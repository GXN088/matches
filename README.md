# matches
Создайте метод с именем , который принимает два аргумента:validateInput1.Строку (text) для проверки2.), которая указывает тип проверкиСтроку (typeМетод должен проверять текст в зависимости от этих типов:"number": должен содержать только цифры (0-9)"word": должен содержать только буквы (a-z или A-Z)"email": должен содержать @ и хотя бы один символ перед ним"phone": должен содержать ровно 10 цифрВозвращаемые сообщения должны быть:Если text равен null: вернуть "Invalid input"Если type недействителен: вернуть "Invalid type"Если проверка пройдена: вернуть "Valid"Если проверка не пройдена: вернуть "Invalid"
